# Certain fields must be renamed before being loaded to the target database

cases_renames = {
    "id_type": "id_type_contagion"
}

type_contagion_renames = {
    "id_type": "id_type_contagion"
}

municipality_renames = {
    "name_municipality": "name"
}